.vercel
.env*.local

# dependencies
/node_modules

# next.js
/.next/
/out/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Club prospect list (PII — lives in Supabase, regenerate from directors-club-db)
scripts/club_prospects.csv

# Send log for the chiropractor batch — a local record of what actually went out
scripts/chiro-batch-sent.json

# Console transcripts from batch runs — local records, like the send log above
scripts/chiro-batch-run-*.log
